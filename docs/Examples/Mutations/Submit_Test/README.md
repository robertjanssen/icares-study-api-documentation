# Submit Test

This folder contains GraphQL mutation examples for starting and submitting study tests.

The available examples include:

- Starting a study test (`start_test`)
- Submitting answers for completed study tests (`submit_test`)

Each example includes:

- a GraphQL request;
- Query Variables;
- notes describing the example.
